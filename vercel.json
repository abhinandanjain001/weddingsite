{
  "version": 2,
  "buildCommand": "echo 'Static site - no build needed'",
  "outputDirectory": "maincel",
  "routes": [
    { "src": "^/(login|signup)$", "dest": "/api/$1" },
    { "src": "^/(?!api/|maincel/).*", "dest": "/maincel$0" },
    { "handle": "filesystem" }
  ]
}
